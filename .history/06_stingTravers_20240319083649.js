

var word="javaScript";
var count=0;
for(let i=0;i<word.length;i++){
    // console.log( word[i]);
    let ch=word.charAt(i);
    if(ch=='a'){
        count=count+1;
    }
    console.log(`Total number of string`);
}

console.log(`character count is ${count}`);